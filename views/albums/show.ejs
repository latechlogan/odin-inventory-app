<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Odin | Inventory App</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <main class="album-details">
      <h1><%= album.album_title %></h1>
      <span>Artist:&ensp;<%= album.artist_name %></span>
      <span>Genre:&ensp;<%= album.genre_name %></span>
      <span>Released:&ensp;<%= album.release_date%></span>
      <div>
        <a href="/albums/<%= album.album_id %>/edit">Edit Details</a>
        <form action="/albums/<%= album.album_id %>/delete" method="post">
          <button
            type="submit"
            onclick="
              return confirm(
                'Are you sure you want to delete this album permanently?'
              );
            "
          >
            Delete Album
          </button>
        </form>
      </div>
    </main>
  </body>
</html>
